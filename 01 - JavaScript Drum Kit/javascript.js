// need to do an onload for a separate javascript file to work 
//Listen for keydown event - when the event happens - run the function
window.addEventListener('keydown', function(element) {
console.log(element);
})
